<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.booklinks_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".add_book_2"></activity>
        <activity android:name=".Add_book_1" />
        <activity android:name=".home_screen" />
        <activity android:name=".forgot_password" />
        <activity android:name=".Success_after_otp" />
        <activity android:name=".login_page" />
        <activity android:name=".otp_page" />
        <activity android:name=".new_account_2" />
        <activity android:name=".new_account_1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>